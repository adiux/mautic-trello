{
  "name": "aiviemarketing/plugin-trello",
  "description": "Add Trello cards from Mautic.",
  "type": "mautic-plugin",
  "license": "GPL-3.0-or-later",
  "authors": [
    {
      "name": "Adrian Schimpf",
      "email": "dev@aivie.ch"
    }
  ],
  "keywords": [
    "mautic",
    "plugin",
    "integration",
    "aivie"
  ],
  "homepage": "https://github.com/aiviemarketing/plugin-trello",
  "support": {
    "issues": "https://github.com/aiviemarketing/plugin-trello/issues",
    "source": "https://github.com/aiviemarketing/plugin-trello",
    "docs": "https://github.com/aiviemarketing/plugin-trello#readme"
  },
  "funding": [
    {
      "type": "website",
      "url": "https://aivie.ch"
    }
  ],
  "require": {
    "php": "^8.1",
      "mautic/core-lib": "^6.0 || ^7.0"
  },
  "extra": {
    "install-directory-name": "AivieTrelloBundle"
  },
  "minimum-stability": "dev",
  "prefer-stable": true,
  "scripts": {
    "test": [
      "@phpunit",
      "@csfixer"
    ],
    "quicktest": [
      "@unit",
      "@csfixer"
    ],
    "phpunit": "../../bin/phpunit -d memory_limit=1G  --bootstrap ../../vendor/autoload.php --configuration phpunit.xml --fail-on-warning --testsuite=all",
    "unit": "../../bin/phpunit -d memory_limit=1G  --bootstrap ../../vendor/autoload.php --configuration phpunit.xml --fail-on-warning --testsuite=unit",
    "functional": "../../bin/phpunit -d memory_limit=1G  --bootstrap ../../vendor/autoload.php --configuration phpunit.xml --fail-on-warning --testsuite=functional",
    "coverage": "../../bin/phpunit -d memory_limit=1G  --bootstrap ../../vendor/autoload.php --configuration phpunit.xml --fail-on-warning --testsuite=all --coverage-text --coverage-html=Tests/Coverage",
    "csfixer": "../../bin/php-cs-fixer fix . -v --dry-run --diff --using-cache=no --config=../../.php-cs-fixer.php",
    "fixcs": "../../bin/php-cs-fixer fix . -v --using-cache=no --config=../../.php-cs-fixer.php",
    "phpstan": [
      "[ -f ../../var/cache/test/AppKernelTestDebugContainer.xml ] || (echo 'Building test cache ...' && APP_ENV=test APP_DEBUG=1 ../../bin/console > /dev/null 2>&1)",
      "php -d memory_limit=4G ../../bin/phpstan analyse -c ../../phpstan.neon AivieTrelloBundle.php Config Controller DependencyInjection EventListener Form Integration Service Tests"
    ]
  }
}
